<ul class="flex justify-between">
  <%= link_to_unless @attachable, page.title, page, data: { turbo_frame: "_top" } %>

  <% if @page_id %>
    <%= link_to "Embed", new_embed_path(page_id: @page_id, embedded_page_id: page.id), data: { turbo_frame: dom_id(Embed.new), turbo_stream: true } %>
  <% end %>
</ul>
